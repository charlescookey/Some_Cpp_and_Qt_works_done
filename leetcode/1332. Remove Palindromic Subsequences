//question actually doesn't make any sense


class Solution {
public:
    int removePalindromeSub(string s) {
        if(s.empty())return 0;
        int l =0 , r = s.size()-1;
        while(l < r){
            if(s[l] != s[r])return 2;
            l++;r--;
        }
        return 1;     
    }
};
